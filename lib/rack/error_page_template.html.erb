<!doctype html>
<head>
  <title>HTML Validation Failed (Rack::HtmlValidator)</title>
  <style>
    body {
      font-size: 12px;
      padding: 2em;
      max-width: 1024px;
      margin: 0 auto;
    }
    body, pre {
      font-family: "Monaco", monospace;
    }
    pre {
      white-space: pre-wrap;
    }
    table {
      border-collapse: collapse;
      border: 1px solid #ccc;
      margin-bottom: 1em;
      width: 100%;
    }
    th, td {
      border-top: 1px solid #ccc;
      padding: 0.5em;
      vertical-align: top;
      text-align: left;
    }
  </style>
</head>
<h1>HTML Validation Failed (Rack::HtmlValidator)</h1>
<h2>Errors</h2>
<%- errors.each do |error| -%>
  <table>
    <tbody>
      <tr>
        <th>Type:</th>
        <td><%= h error.type %></td>
      </tr>
      <tr>
        <th>Line:</th>
        <td><%= h error.line %></td>
      </tr>
      <tr>
        <th>Message:</th>
        <td><%= h error.message %></td>
      </tr>
      <tr>
        <th>Source:</th>
        <td><pre><%= h error.source %></pre></td>
      </tr>
    </tbody>
  </table>
<%- end -%>
<h2>Source</h2>
<table>
  <tbody>
    <tr>
      <th>Status</td>
      <td><%= h response[0] %></td>
    </tr>
    <tr>
      <th>Header</td>
      <td>
        <ul>
          <%- response[1].each do |k, v| -%>
            <li><%= h "#{k}: #{v}" %></li>
          <%- end -%>
        </ul>
      </td>
    </tr>
    <tr>
      <th>Body</td>
      <td><pre><%= h response[2].join %></pre></td>
    </tr>
  </tbody>
<table>
